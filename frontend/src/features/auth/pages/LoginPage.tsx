export function LoginPage() {
  return (
    <main className="page stack">
      <h1>Feedo</h1>
      <section className="panel">
        <h2>Sign in</h2>
        <p className="muted">Auth is handled by django-allauth Headless.</p>
      </section>
    </main>
  );
}
